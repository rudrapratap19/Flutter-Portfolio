name: portfolio
description: "Rudra Pratap Singh — Flutter & AI Developer Portfolio"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  animated_text_kit: ^4.2.2
  flutter_animate: ^4.5.0
  http: ^1.2.0
  fl_chart: ^0.70.2
  visibility_detector: ^0.4.0+2
  font_awesome_flutter: ^11.0.0
  google_generative_ai: ^0.4.7
  flutter_markdown_plus: ^1.0.12
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/Certificates/Certificates/
